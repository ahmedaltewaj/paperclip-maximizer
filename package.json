{
  "name": "paperclip-maximizer",
  "version": "1.0.0",
  "description": "Universal Paperclip Production System - An incremental game about maximizing paperclip production",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo 'Tests: Build verification' && npm run build && echo '✓ Build successful'"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}